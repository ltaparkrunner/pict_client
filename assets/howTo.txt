1. переделать на кнопку File dialog, переделать работу строки. https://minio:9000/ не работает
только http://minio:9000 переделать на /net
2. Возникли следующие предупреждения при разборе информации о типах QML библиотеки /home/alex-m/wrk/myproj/qt/pict_client/build_dbg/pict_client:
File "/home/alex-m/wrk/myproj/qt/pict_client/build_dbg/pict_client/pictStore.qmltypes" does not exist.
3. does not work 2 places: first: both then the new folders creates one and the second, review this.
they both are in client 100 percent.. ../ folder in new virtual folder
4. Когдв открывается картинка. Как работает диалоговое окно
5. убрать комментарии.
6. убрать не закрывать диалог, переделать диалоги, переделать кнопки
7. переписывание между двумя сетевыми папками(файлами), или двумя дисковыми
8. разные tokenы

1. переделать addresses lines
2. it looks like it does not check if token is expired

по 7 пункту:

1. Комментарии и сообщения убрать
2. Кнопка
3. Строка при загрузке
4. не закрывать диалог
5. переписывание между двумя сетевыми папками(файлами), или двумя дисковыми
6. Удаление по другому
7. local files and net files behave differently -- fix it.
8. not understand if token expired

1. network tab doesnot work. local folder was recorded to network?
2. after write file from local to net folder the wrong networkPath



